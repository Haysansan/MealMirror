/// Placeholder for a legacy DAO layer.
///
/// The current app uses lightweight local persistence via SharedPreferences.
/// This file is kept (intentionally minimal) to avoid breaking imports while
/// iterating on storage.
class PetDao {
  const PetDao();
}
