/// Placeholder for a legacy table model.
///
/// The current app uses lightweight local persistence via SharedPreferences.
/// This file is intentionally kept (minimal) to avoid breaking imports while
/// iterating on storage.
class Meals {
  const Meals();
}
